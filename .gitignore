# NEVER upload the token — it lives in GitHub Secrets instead
config.txt

# local tooling, not part of the repo
gh
gh.zip

# Cloudflare worker
cf-worker/node_modules/
cf-worker/.webhook_secret
cf-worker/seed-state.json
cf-worker/.wrangler/
